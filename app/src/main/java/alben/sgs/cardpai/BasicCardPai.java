package alben.sgs.cardpai;

import alben.sgs.type.Type;

public class BasicCardPai extends CardPai {
	public BasicCardPai(Type.CardPai na, Type.CardPaiClass c, int n,
			int imgNumber) {
		super(na, c, n, imgNumber);
	}
}
